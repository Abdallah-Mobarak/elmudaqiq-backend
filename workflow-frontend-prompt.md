# مهمة الفرونت: تحديث مسار عمل العقود (Workflow)

اتعدّل مسار العقود ليطابق كلام صاحب العمل. المطلوب تعديل الواجهة حسب الآتي.

## المسار الجديد (الكامل)
```
السكرتارية تنشئ العقد
  → مدير المراجعة  (يعتمد → تدقيق فني | أو يرجّع للسكرتارية بملاحظات)
    → التدقيق الفني  (يرفع الميزان + التسويات ثم يرحّل)
      → إدارة الجودة (QC)  (يعتمد → الشريك الإداري | أو يرجّع للتدقيق الفني بملاحظات)
        → الشريك الإداري  (يعتمد → الأرشفة | أو يرجّع للتدقيق الفني بملاحظات)
          → الأرشيف  (يرحّل → مكتمل)
```
> **اتشال من المسار:** التدقيق الميداني (Field Audit) والإيداع التنظيمي (Regulatory Filing) — مبقوش في المسار الأساسي.

قيم `workflowStage` بالترتيب:
`PENDING_AUDIT_MANAGER → PENDING_TECHNICAL_AUDIT → PENDING_QC_REVIEW → PENDING_PARTNER_REVIEW → PENDING_ARCHIVING → COMPLETED`

---

## 1) ترحيل المرحلة للأمام (submit)
**`PATCH /engagement-contracts/:id/submit-stage`** (بدون body)
- كل دور يرحّل من مرحلته للي بعدها تلقائياً حسب دوره:
  - المدقق الفني: `PENDING_TECHNICAL_AUDIT → PENDING_QC_REVIEW` (لازم ميزان المراجعة يكون **معتمد** الأول، وإلا خطأ 400).
  - إدارة الجودة: `PENDING_QC_REVIEW → PENDING_PARTNER_REVIEW`
  - الشريك الإداري: `PENDING_PARTNER_REVIEW → PENDING_ARCHIVING`
  - الأرشيف: `PENDING_ARCHIVING → COMPLETED`
- الرد: `{ message, data: contract }`. الأخطاء: `{ customMessage }` بعربي واضح.

## 2) الإرجاع للتدقيق الفني بملاحظات (جديد)
**`PATCH /engagement-contracts/:id/return-to-technical`** body:
```json
{ "comments": "الملاحظات المطلوب تعديلها" }
```
- متاح لـ **إدارة الجودة** (من `PENDING_QC_REVIEW`) و**الشريك الإداري** (من `PENDING_PARTNER_REVIEW`).
- بيرجّع العقد لمرحلة `PENDING_TECHNICAL_AUDIT` ويشعر المدقق الفني بالملاحظات.
- الرد: `{ message, data: contract }`.
- في الواجهة: في شاشة الجودة/الشريك، زرارين: **"اعتماد وترحيل"** (submit-stage) و**"إرجاع للتدقيق الفني"** (يفتح popup للملاحظات → return-to-technical).

## 3) الأدوار والصلاحيات (مهم)
اتضافت صلاحيات للأدوار بعد التدقيق الفني (كانت مقفولة قبل كده):
- `QUALITY_CONTROL`, `MANAGING_PARTNER`, `ARCHIVE`, `FIELD_AUDITOR`, `ASSISTANT_TECHNICAL_AUDITOR` → دلوقتي عندهم **عرض العقود** (`VIEW_CONTRACTS`) ويقدروا يرحّلوا مرحلتهم.
- كل دور بيشوف العقود في مرحلته وما بعدها فقط (الباك بيفلتر تلقائياً حسب الدور).
- **تأكد إن أسماء الأدوار في النظام مطابقة بالظبط:** `TECHNICAL_AUDITOR`, `QUALITY_CONTROL`, `MANAGING_PARTNER`, `ARCHIVE` (لو الاسم مختلف في الداتا، الصلاحيات مش هتشتغل).

## 4) التدقيق الفني — تعديل بعد الاعتماد
المدقق الفني يقدر يعدّل تسويات ميزان المراجعة (مدين/دائن التسوية) **في أي وقت** — حتى بعد اعتماد الميزان وحتى لو العقد اتنقل لمرحلة أبعد. باقي الأدوار مقيّدين بمرحلة التدقيق الفني وقبل الاعتماد.
- Endpoint: `PATCH /contracts/trial-balance/accounts/:accountId` (نفس الموجود).

---
## ملخص التغييرات للواجهة
1. شيل خطوات **التدقيق الميداني** و**الإيداع التنظيمي** من عرض المسار.
2. ضيف زر **"إرجاع للتدقيق الفني بملاحظات"** في شاشتَي الجودة والشريك الإداري.
3. اتأكد إن شاشات الأدوار الجديدة (جودة/شريك/أرشيف) بتظهر وبتشتغل بعد إضافة الصلاحيات.
4. رسائل الأخطاء اعرض الـ `customMessage`/`message` القادم من الباك مباشرة.
